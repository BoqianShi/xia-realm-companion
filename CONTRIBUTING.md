# 贡献

欢迎交互改进、规则修复、无障碍改进和原创测试。

1. 按 README 启动本地服务。
2. 报告操作、预期和实际结果，使用原创数据，移除团存档和个人信息。
3. 计算变更说明依据、条件与验证；不要附官方整页原文、扫描图或商业模组。
4. 运行 `npm run typecheck`、`npm test`、`npm run build`；暂存改动后运行 `npm run check:public`。
5. PR 说明影响和限制。自有贡献按 MIT 发布，第三方材料明确归属许可。

私有导入留在 `private-content/` 或仓库外，勿强制添加忽略目录。框架在此维护，各团资料包独立维护。

界面按高频任务排序，不把新功能默认置顶。手动记账和只读计算分开，不悄悄改变状态、资源或回合。
